# projects

## Publishing future workspace files

Use the helper script to add, commit, and push all workspace files to the `main` branch:

```bash
cd /workspaces/projects
./publish_main.sh "Your commit message"
```

If you omit the message, it defaults to `Update workspace files`.
